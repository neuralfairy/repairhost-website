import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Header } from "@/components/header"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Repairhost Solutions - CRM Implementation & Optimization",
  description:
    "CRM, Calibrated: From Setup to Scale. Transform your customer relationship systems with enterprise CRM consulting and implementation services.",
  keywords: "CRM setup, sales process optimization, system integration, crm implementation, B2B sales tools",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
